# 方案

## 总体策略

采用“PC 后台先行 + 小程序最小登录同步验证”的双线方案：

- **PC 网页端**：先做完整登录后台，作为管理入口与验证平台。
- **小程序端**：同步做最小登录测试版，只验证微信登录闭环。
- **APP**：当前阶段不做。
- **数据大屏**：当前阶段不做，先用 PC 统计页替代。

## 小程序第一阶段范围（只做最小功能）

仅做 5 个页面：

1. 登录页
2. 绑定手机号预留页
3. 身份选择页
4. 农户测试首页
5. 服务方测试首页

不做：服务列表、地图、订单、消息、AI 导航、复杂 UI 设计、APP。

## PC 网页端当前阶段实现范围

访问路径：`/admin`

优先实现：

1. 账号密码登录
2. 当前用户接口
3. 动态菜单
4. 角色权限
5. 登录日志
6. 用户管理
7. 角色管理
8. 菜单管理

## 小程序登录闭环实现范围

1. 打开小程序进入登录页
2. 点击微信登录
3. 获取微信 code
4. 请求后端 `/api/auth/wechat-login`
5. 后端返回 token
6. 小程序保存 token
7. 请求 `/api/auth/current-user`
8. 显示当前用户信息
9. 根据角色跳转：
   - FARMER → 农户测试首页
   - PROVIDER → 服务方测试首页
10. 退出登录

## 推荐开发顺序

### 第一步：PC 登录后台

先完成：

- `/admin` 登录页
- `/dashboard` 首页
- `/system/user` 用户管理
- `/system/role` 角色管理
- `/system/menu` 菜单管理
- `/system/login-log` 登录日志

阶段目标：admin 可登录；不同角色可见不同菜单；登录日志可查询。

### 第二步：最小小程序同步接入

在登录接口可用后，完成：微信登录、保存 token、current-user、退出登录、角色跳转。

阶段目标：验证微信生态能力与后端认证链路可用。

### 第三步：服务方入驻闭环

- 小程序：服务方提交入驻资料
- PC 后台：运营审核
- 小程序：查看审核状态
