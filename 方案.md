# 方案（在保留登录闭环基础上新增服务方入驻闭环）

## 0. 实施原则

1. 第一闭环（登录认证）不删除、不降级。
2. 第二闭环（服务方入驻）以新增方式接入。
3. 先保证主链路可跑通，再逐步替换为数据库与真实小程序。

---

## 1. 第一闭环（保留方案）

保留现有能力：

- `POST /api/auth/login`
- `POST /api/auth/wechat-login`
- `GET /api/auth/current-user`
- `POST /api/auth/logout`
- `GET /api/system/login-log`

保留页面：

- PC：`/admin`、`/dashboard`
- 小程序测试：`/mini/login`

---

## 2. 第二闭环（新增方案）

### 2.1 状态模型

- `NOT_SUBMITTED`
- `PENDING`
- `APPROVED`
- `REJECTED`
- `DISABLED`

### 2.2 数据模型（内存态最小实现）

- `PROVIDER_APPLIES`：入驻申请
- `PROVIDERS`：审核通过后的主体
- `PROVIDER_AUDIT_LOGS`：审核日志

### 2.3 后端接口（新增）

小程序侧：

- `GET /api/provider/apply/status`
- `POST /api/provider/apply/submit`
- `GET /api/provider/apply/detail`
- `POST /api/provider/apply/resubmit`
- `GET /api/provider/workbench`

PC 侧：

- `GET /api/admin/provider/apply/list`
- `GET /api/admin/provider/apply/detail`
- `POST /api/admin/provider/apply/approve`
- `POST /api/admin/provider/apply/reject`
- `GET /api/admin/provider/list`
- `GET /api/admin/provider/detail`

上传：

- `POST /api/file/upload`

### 2.4 页面改造（新增）

小程序：

- `frontend/mini/provider-onboard.html`（入驻申请）
- `frontend/mini/provider-apply-status.html`（审核中）
- `frontend/mini/provider-apply-reject.html`（驳回详情）
- `frontend/mini/provider-home.html`（工作台，仅通过可进入）
- `frontend/mini/login.html`（登录后先查 `apply/status` 决定跳转）

PC：

- `frontend/admin/provider-audit.html`（列表、详情、通过、驳回）

---

## 3. 开发顺序

1. 保留第一闭环并回归验证。
2. 新增第二闭环后端状态接口。
3. 新增小程序入驻申请/状态/驳回/工作台页面。
4. 新增 PC 审核列表与审核动作。
5. 脚本覆盖第二闭环验收。

---

## 4. 完成标准

- 第一闭环仍可完整运行。
- 第二闭环可跑通“提交→审核→状态回传→通过后进工作台”。
- 驳回后可重提，且驳回原因可见。
