# 方案（保留前两闭环并完成第三闭环）

## 0. 原则
1. 第一闭环与第二闭环不删除不降级。
2. 第三闭环以新增方式接入。
3. 继续采用内存态模型打通主流程。

## 1. 第三闭环新增数据模型（内存态）
- `SERVICE_CATEGORIES`：服务分类字典
- `SERVICE_ABILITIES`：服务能力
- `SERVICE_CONTACT_LOGS`：联系记录

## 2. 第三闭环后端接口
### 服务方端
- `GET /api/provider/ability/list`
- `POST /api/provider/ability/create`
- `POST /api/provider/ability/update`
- `POST /api/provider/ability/enable`
- `POST /api/provider/ability/disable`
- `GET /api/provider/ability/detail`

### 农户端
- `GET /api/farmer/service/category/list`
- `GET /api/farmer/provider/list`
- `GET /api/farmer/provider/detail`
- `POST /api/farmer/contact-log/create`

### PC 后台
- `GET /api/admin/service-ability/list`
- `GET /api/admin/service-ability/detail`
- `POST /api/admin/service-ability/disable`
- `GET /api/admin/contact-log/list`
- `GET /api/admin/contact-log/stat`

## 3. 页面新增
### 小程序测试页
- `frontend/mini/farmer-index.html`
- `frontend/mini/farmer-service-list.html`
- `frontend/mini/farmer-provider-detail.html`
- `frontend/mini/provider-ability-list.html`
- `frontend/mini/provider-ability-edit.html`
- `frontend/mini/provider-ability-detail.html`

### PC 后台页
- `frontend/admin/service-ability.html`
- `frontend/admin/contact-log.html`

## 4. 核心规则
1. 服务方维护能力前置校验：角色 PROVIDER + 申请状态 APPROVED。
2. 农户列表仅返回：服务方 APPROVED + 服务能力 ENABLE。
3. 联系记录创建前校验能力状态 ENABLE。

## 5. 完成标准
- 服务方可维护能力（增改启停查）。
- 农户可按分类找服务、看详情、发起联系。
- 联系行为有日志，后台可查看能力与联系记录。


## 6. 对齐整改项（本轮）
1. 农户列表按业务规则排序并返回资料完整度字段。
2. 后台服务能力列表补充按类型/服务方/状态筛选。
3. 后台联系记录补充按服务方/服务类型筛选。
