# 方案（第二个闭环：服务方入驻闭环）

## 实施策略

采用“内存态数据 + 静态页面 + FastAPI 接口”的最小实现方案，先打通完整业务闭环，再逐步替换为数据库与真实小程序。

## 状态模型

- `NOT_SUBMITTED`：未提交
- `PENDING`：审核中
- `APPROVED`：审核通过
- `REJECTED`：审核驳回
- `DISABLED`：禁用（第一版先保留状态）

## 数据模型（内存态）

- `PROVIDER_APPLIES`：入驻申请记录
- `PROVIDERS`：审核通过后的服务方主体
- `PROVIDER_AUDIT_LOGS`：审核记录

## 后端改造清单

1. 新增小程序接口：
   - `GET /api/provider/apply/status`
   - `POST /api/provider/apply/submit`
   - `GET /api/provider/apply/detail`
   - `POST /api/provider/apply/resubmit`
   - `GET /api/provider/workbench`
2. 新增 PC 审核接口：
   - `GET /api/admin/provider/apply/list`
   - `GET /api/admin/provider/apply/detail`
   - `POST /api/admin/provider/apply/approve`
   - `POST /api/admin/provider/apply/reject`
   - `GET /api/admin/provider/list`
   - `GET /api/admin/provider/detail`
3. 新增文件上传接口：
   - `POST /api/file/upload`

## 页面改造清单

### 小程序页面

- `frontend/mini/login.html`：登录后按 `apply/status` 跳转。
- `frontend/mini/provider-onboard.html`：入驻申请页（提交/重提）。
- `frontend/mini/provider-apply-status.html`：审核中页面。
- `frontend/mini/provider-apply-reject.html`：驳回详情页。
- `frontend/mini/provider-home.html`：服务方工作台（仅通过可进入）。

### PC 页面

- `frontend/admin/provider-audit.html`：申请列表 + 查看详情 + 审核通过 + 审核驳回。

## 开发顺序

1. 先补后端状态与审核接口。
2. 再改小程序状态跳转和申请提交。
3. 再改 PC 审核列表与审核动作。
4. 最后补自测脚本，验证提交→审核→状态回传→工作台准入。

## 完成标准（本阶段）

- 通过新接口跑通提交、审核通过、审核驳回、重提、工作台准入控制。
- 小程序与 PC 页面均基于新接口闭环运行。
- 自测脚本验证核心链路通过。
