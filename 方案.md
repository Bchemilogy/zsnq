# 方案

## 总体策略

采用“PC 后台先行 + 小程序最小登录同步验证”的双线方案：

- **PC 网页端**：先做完整登录后台，作为管理入口与验证平台。
- **小程序端**：同步做最小登录测试版，只验证微信登录闭环。
- **APP**：当前阶段不做。
- **数据大屏**：当前阶段不做，先用 PC 统计页替代。

## 小程序第一阶段范围（只做最小功能）

仅做 5 个页面：

1. 登录页
2. 绑定手机号预留页
3. 身份选择页
4. 农户测试首页
5. 服务方测试首页

不做：服务列表、地图、订单、消息、AI 导航、复杂 UI 设计、APP。

## PC 网页端当前阶段实现范围

访问路径：`/admin`

优先实现：

1. 账号密码登录
2. 当前用户接口
3. 动态菜单
4. 角色权限
5. 登录日志
6. 用户管理
7. 角色管理
8. 菜单管理

## 小程序登录闭环实现范围

1. 打开小程序进入登录页
2. 点击微信登录
3. 获取微信 code
4. 请求后端 `/api/auth/wechat-login`
5. 后端返回 token
6. 小程序保存 token
7. 请求 `/api/auth/current-user`
8. 显示当前用户信息
9. 根据角色跳转：
   - FARMER → 农户测试首页
   - PROVIDER → 服务方测试首页
10. 退出登录

## 推荐开发顺序

### 第一步：PC 登录后台

先完成：

- `/admin` 登录页
- `/dashboard` 首页
- `/system/user` 用户管理
- `/system/role` 角色管理
- `/system/menu` 菜单管理
- `/system/login-log` 登录日志

阶段目标：admin 可登录；不同角色可见不同菜单；登录日志可查询。

### 第二步：最小小程序同步接入

在登录接口可用后，完成：微信登录、保存 token、current-user、退出登录、角色跳转。

阶段目标：验证微信生态能力与后端认证链路可用。

### 第三步：服务方入驻闭环

- 小程序：服务方提交入驻资料
- PC 后台：运营审核
- 小程序：查看审核状态

## 当前落地进度（2026-04-28）

已完成最小后端原型（用于第一阶段联调）：

- `POST /api/auth/login`（PC 账号密码登录）
- `POST /api/auth/wechat-login`（小程序微信登录测试）
- `GET /api/auth/current-user`（当前用户与角色菜单）
- `POST /api/auth/logout`（退出登录）
- `GET /api/system/login-log`（登录日志查询）
- `GET /api/system/login-log-summary`（登录日志按端统计）
- `GET /api/system/dashboard-stats`（后台首页统计）
- `GET /admin`（PC 后台登录页）
- `GET /dashboard`（PC 后台最小首页）
- `GET /system/login-log`（PC 后台登录日志页）
- `GET /system/user`（PC 后台用户管理页）
- `GET /api/system/user`（用户列表接口，仅 ADMIN）
- `GET /system/role`（PC 后台角色管理页）
- `GET /system/menu`（PC 后台菜单管理页）
- `GET /api/system/role`（角色列表接口，仅 ADMIN）
- `GET /api/system/menu`（菜单列表接口，仅 ADMIN）
- `GET /mini/login`（小程序登录测试页）
- `GET /mini/bind-phone`（绑定手机号预留页）
- `GET /mini/role-select`（身份选择页）
- `GET /mini/farmer-home`（农户测试首页）
- `GET /mini/provider-home`（服务方测试首页）
- `GET /mini/provider-onboard`（服务方入驻申请页）
- `POST /api/provider/onboard-submit`（服务方提交入驻）
- `GET /api/provider/onboard-status`（服务方查看入驻状态）
- `GET /operate/provider-audit`（PC 运营审核页）
- `GET /api/admin/provider-onboard`（PC 查看入驻列表）
- `POST /api/admin/provider-onboard/{id}/audit`（PC 审核动作）
- 后台首页菜单改为按 `current-user.menus` 动态渲染
- 新增 `scripts/smoke_test.py` 覆盖关键链路自测（登录、入驻、审核、状态回看）
- 新增 `scripts/acceptance_check.py` 与 `验收.md`，用于第一阶段 10 项验收对照检查
