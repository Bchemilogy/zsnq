# 监管统计闭环代码检查文件

> 适用范围：第五个闭环“监管统计闭环”代码开发完成后的逐步检查。  
> 闭环目标：政府账号登录后，可以查看服务统计、查看服务记录、查看凭证，并导出补贴参考清单。  
> 使用对象：让 AI 或开发人员在每完成一个开发步骤后，根据本文件检查代码是否存在明显逻辑错误、权限漏洞、数据遗漏或功能未实现问题。

---

## 0. 使用方式

每完成一个步骤的代码后，按以下流程检查：

```text
1. 确认本步骤涉及的后端接口、前端页面、数据库表、权限配置是否都已实现。
2. 对照本文件对应章节逐项检查。
3. 对每一项标记：通过 / 不通过 / 待确认。
4. 如果发现问题，先修复，再进入下一步骤。
5. 每个步骤完成后，至少运行一次接口测试或页面测试。
```

建议让 AI 使用下面的固定提示词进行检查：

```text
请根据《监管统计闭环代码检查文件》检查我刚完成的代码。
重点判断：
1. 是否实现了当前步骤的功能要求；
2. 是否存在明显逻辑错误；
3. 是否存在权限越权、数据泄露、SQL 注入、文件越权下载等安全问题；
4. 是否符合本项目“政府只读、运营可管理、农户和服务方数据隔离”的权限原则；
5. 请按“问题位置、问题原因、修改建议、风险等级”输出检查结果。
```

---

## 1. 总体检查原则

### 1.1 功能完整性原则

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| 政府账号能登录 | GOV_ADMIN 账号可正常登录 PC 后台 |  |
| 政府账号菜单正确 | 只显示政府监管相关菜单 |  |
| 服务统计可查看 | 能看到服务方数量、需求数量、服务记录数量、处理量等统计 |  |
| 服务记录可查询 | 能按时间、区域、服务类型、服务方筛选服务记录 |  |
| 凭证可查看 | 能查看作业照片、合同、票据等凭证 |  |
| 补贴参考清单可导出 | 能导出 Excel 文件 |  |
| 登录与操作有日志 | 登录、查看、导出等关键行为有记录 |  |

### 1.2 权限安全原则

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| 未登录拦截 | 未带 token 访问接口返回 401 |  |
| 越权拦截 | 非 GOV_ADMIN / OPS_ADMIN 访问政府统计接口返回 403 |  |
| 政府只读 | 政府账号不能新增、修改、删除业务数据 |  |
| 农户数据隔离 | 农户不能查看其他农户需求和凭证 |  |
| 服务方数据隔离 | 服务方不能查看其他服务方记录 |  |
| 文件访问控制 | 凭证文件不能通过猜测 URL 任意访问敏感文件 |  |
| 导出权限控制 | 只有授权角色能导出补贴参考清单 |  |

### 1.3 数据准确性原则

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| 统计口径明确 | 统计数据只统计有效服务记录，排除已取消、已删除数据 |  |
| 处理量准确 | 面积 / 吨数汇总字段使用实际处理量，不使用需求量代替 |  |
| 时间范围准确 | 查询条件包含开始时间和结束时间，边界处理正确 |  |
| 区域筛选准确 | 按区域筛选时能正确包含下级乡镇 / 村数据，或明确只查当前区域 |  |
| 服务类型准确 | 服务类型枚举与字典一致 |  |
| 软删除过滤 | 已逻辑删除数据不参与统计和导出 |  |

---

## 2. 第一步：政府账号登录与菜单权限检查

### 2.1 功能要求

```text
政府监管人员使用 PC 账号密码登录；
登录后进入政府监管首页；
只能看到政府监管相关菜单；
不能看到运营审核、用户管理、角色管理、系统配置等菜单。
```

### 2.2 后端检查

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| 用户角色存在 | 数据库存在 GOV_ADMIN 角色 |  |
| 账号角色绑定正确 | 政府测试账号已绑定 GOV_ADMIN |  |
| 登录接口返回角色 | password-login 返回 roles 包含 GOV_ADMIN |  |
| current-user 返回菜单 | current-user 返回政府菜单和权限 |  |
| 菜单来源正确 | 菜单来自角色权限表，而不是前端写死 |  |
| 禁用账号拦截 | 政府账号 status=DISABLED 时不能登录 |  |
| token 校验正确 | token 过期或错误时返回 401 |  |

### 2.3 前端检查

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| 登录页可用 | 输入政府账号密码可登录 |  |
| 菜单显示正确 | 只显示政府监管菜单 |  |
| 刷新状态保持 | 刷新页面后仍能通过 current-user 恢复菜单 |  |
| 无权限页面处理 | 手动访问无权限路由时显示 403 或跳转无权限页 |  |
| 退出登录正常 | 退出后清除 token 并返回登录页 |  |

### 2.4 安全漏洞检查

| 检查项 | 风险说明 | 结果 |
|---|---|---|
| 前端隐藏不等于权限 | 后端接口必须再次校验权限 |  |
| GOV_ADMIN 不能访问运营审核接口 | 手动请求审核接口应返回 403 |  |
| GOV_ADMIN 不能访问系统用户管理接口 | 手动请求用户管理接口应返回 403 |  |
| token 不应存明文敏感信息 | token 内不应包含密码、openid、身份证号等敏感信息 |  |

---

## 3. 第二步：服务统计功能检查

### 3.1 功能要求

```text
政府账号登录后，可以查看区域内服务统计，包括：
1. 服务方总数；
2. 农户需求总数；
3. 服务记录总数；
4. 已完成服务数量；
5. 作业面积 / 处理量汇总；
6. 按服务类型统计；
7. 按区域统计；
8. 按时间趋势统计。
```

### 3.2 后端接口检查

建议接口：

```text
GET /api/gov/stat/overview
GET /api/gov/stat/service-type
GET /api/gov/stat/region
GET /api/gov/stat/trend
```

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| 接口需要登录 | 不带 token 返回 401 |  |
| 接口需要政府权限 | 非 GOV_ADMIN / OPS_ADMIN 返回 403 |  |
| 支持时间筛选 | 支持 startTime、endTime |  |
| 支持区域筛选 | 支持 regionCode |  |
| 支持服务类型筛选 | 支持 serviceType |  |
| 统计口径正确 | 只统计 COMPLETED 或有效记录 |  |
| 排除取消需求 | CANCELLED 不计入完成服务统计 |  |
| 排除禁用服务方 | DISABLED 服务方按业务要求处理，不能错误计入有效服务主体 |  |
| 防止空指针 | 无数据时返回 0，不报错 |  |
| 数值类型正确 | 面积、吨数使用 BigDecimal / Decimal，不使用浮点误差大的类型 |  |

### 3.3 SQL / 查询逻辑检查

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| COUNT 不重复 | 多表 JOIN 时避免一条记录因多个凭证被重复统计 |  |
| SUM 不重复 | 汇总处理量时避免 JOIN evidence 导致重复累加 |  |
| 时间边界正确 | endTime 当天数据不被漏掉 |  |
| 软删除过滤 | deleted_flag=1 的数据不统计 |  |
| 服务类型字典一致 | 数据库枚举与前端展示名称匹配 |  |
| 分页不影响统计 | 统计接口不能只统计当前分页数据 |  |

### 3.4 前端页面检查

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| 统计卡片显示 | 显示服务方数、需求数、记录数、处理量 |  |
| 筛选条件可用 | 时间、区域、服务类型筛选能触发重新查询 |  |
| 空数据处理 | 无数据时显示 0 或暂无数据 |  |
| 加载状态 | 请求中有 loading 提示 |  |
| 异常提示 | 接口失败时有错误提示 |  |
| 数字格式清晰 | 面积、吨数保留合理小数位 |  |

---

## 4. 第三步：服务记录查询功能检查

### 4.1 功能要求

```text
政府账号可以查看服务记录列表，并按条件筛选。
可查看字段包括：服务方、农户、服务类型、区域、处理量、服务时间、凭证数量、记录状态等。
```

### 4.2 后端接口检查

建议接口：

```text
GET /api/gov/service-record/list
GET /api/gov/service-record/detail
```

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| 登录校验 | 未登录返回 401 |  |
| 权限校验 | 非政府 / 运营角色返回 403 |  |
| 支持分页 | pageNum、pageSize 生效 |  |
| 支持筛选 | serviceType、providerName、regionCode、startTime、endTime 生效 |  |
| 支持详情 | detail 能返回完整服务记录 |  |
| 数据不重复 | 列表中同一服务记录不因凭证 JOIN 重复出现 |  |
| 敏感信息处理 | 政府端农户电话可按要求脱敏 |  |
| 排序正确 | 默认按提交时间或服务时间倒序 |  |

### 4.3 前端页面检查

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| 列表字段完整 | 显示记录编号、服务方、服务类型、处理量、时间、状态 |  |
| 分页正常 | 切换页码能请求对应数据 |  |
| 筛选正常 | 输入筛选条件后结果变化正确 |  |
| 详情入口可用 | 点击查看详情能打开详情页 |  |
| 状态显示清晰 | COMPLETED、SUBMITTED 等状态有中文显示 |  |
| 无数据友好 | 无记录时展示“暂无数据” |  |

### 4.4 逻辑漏洞检查

| 检查项 | 风险说明 | 结果 |
|---|---|---|
| 不能直接信任前端 regionCode | 后端要根据政府账号可查看区域限制数据范围 |  |
| 不能通过修改 recordId 查看越权数据 | 后端 detail 接口也要校验权限和区域范围 |  |
| 不能暴露全部手机号 | 按需求决定是否脱敏，至少政府端应谨慎展示 |  |
| 不能返回文件真实物理路径 | 只返回可控 fileId 或受保护访问地址 |  |

---

## 5. 第四步：凭证查看与证据链检查

### 5.1 功能要求

```text
政府账号可以查看某条服务记录的证据链，包括：
1. 农户需求信息；
2. 服务方信息；
3. 服务记录信息；
4. 作业照片；
5. 合同 / 协议；
6. 处理量；
7. 状态流转日志。
```

### 5.2 后端接口检查

建议接口：

```text
GET /api/gov/evidence-chain/detail?recordId=xxx
GET /api/gov/evidence/file-preview?fileId=xxx
```

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| 证据链数据完整 | 返回需求、服务方、记录、凭证、状态日志 |  |
| recordId 必填校验 | recordId 为空时返回明确错误 |  |
| recordId 存在校验 | 不存在时返回 404 或业务错误 |  |
| 权限校验 | 非授权角色不能查看 |  |
| 区域权限校验 | 政府账号只能看授权区域数据 |  |
| 文件权限校验 | 只能查看该记录关联的文件 |  |
| 文件类型校验 | 只允许预览 jpg、png、pdf 等允许类型 |  |
| 文件地址安全 | 不直接暴露服务器绝对路径 |  |

### 5.3 前端页面检查

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| 需求信息展示 | 农户提交的地点、数量、时间、备注可见 |  |
| 服务方信息展示 | 服务方名称、服务类型、区域可见 |  |
| 服务记录展示 | 实际处理量、开始结束时间、说明可见 |  |
| 凭证图片预览 | 作业照片能预览 |  |
| 合同文件查看 | PDF 或图片合同能打开 / 下载 |  |
| 状态流转展示 | 已提交、已联系、服务中、已完成时间线可见 |  |
| 文件缺失处理 | 无合同或无照片时有明确提示 |  |

### 5.4 安全漏洞检查

| 检查项 | 风险说明 | 结果 |
|---|---|---|
| 禁止任意文件下载 | 不能通过 fileUrl 参数下载任意路径文件 |  |
| 禁止越权查看凭证 | 修改 fileId 不能查看无权限凭证 |  |
| 合同和证件注意脱敏 | 身份证、营业执照等敏感材料不应无控制暴露 |  |
| 文件预览接口必须鉴权 | 不能匿名访问敏感凭证 |  |
| 防止路径穿越 | 文件参数不能包含 ../ 等路径穿越风险 |  |

---

## 6. 第五步：导出补贴参考清单检查

### 6.1 功能要求

```text
政府账号可以根据时间、区域、服务类型等条件导出补贴参考清单。
导出内容应来源于已完成服务记录和凭证数据。
平台只提供参考清单，不做补贴自动审批。
```

### 6.2 建议导出字段

| 字段 | 说明 |
|---|---|
| 序号 | 导出序号 |
| 服务记录编号 | recordId |
| 需求编号 | demandId |
| 服务方名称 | providerName |
| 服务方联系人 | contactName |
| 服务类型 | serviceTypeName |
| 服务区域 | regionName |
| 服务地点 | serviceAddress |
| 实际处理量 | processQuantity |
| 单位 | unit |
| 开始时间 | startTime |
| 结束时间 | endTime |
| 作业照片数量 | photoCount |
| 合同数量 | contractCount |
| 凭证完整性 | complete / incomplete |
| 备注 | remark |

### 6.3 后端接口检查

建议接口：

```text
GET /api/gov/subsidy/export
```

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| 导出需要登录 | 未登录返回 401 |  |
| 导出需要权限 | 非 GOV_ADMIN / OPS_ADMIN 返回 403 |  |
| 查询条件生效 | 时间、区域、服务类型筛选正确 |  |
| 只导出有效记录 | 默认只导出 COMPLETED 服务记录 |  |
| 导出数据不重复 | 一条服务记录只导出一行 |  |
| 凭证数量准确 | 照片、合同数量统计正确 |  |
| 文件名合理 | 文件名包含“补贴参考清单”和日期 |  |
| Content-Type 正确 | Excel 下载响应头正确 |  |
| 大数据量处理 | 数据量较大时不应内存溢出，至少有数量限制或异步方案预留 |  |
| 导出日志记录 | 导出人、时间、条件、数量有日志 |  |

### 6.4 Excel 内容检查

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| 表头完整 | 字段名称清楚 |  |
| 中文正常 | 不乱码 |  |
| 时间格式正确 | yyyy-MM-dd HH:mm:ss |  |
| 数值格式正确 | 处理量为数字格式 |  |
| 单位清楚 | 亩 / 吨 / 次明确 |  |
| 空值处理 | 空字段显示为空或“-”，不显示 null |  |
| 文件可打开 | WPS / Excel 可正常打开 |  |

### 6.5 安全漏洞检查

| 检查项 | 风险说明 | 结果 |
|---|---|---|
| 防止批量越权导出 | 后端必须按账号权限限制导出范围 |  |
| 防止导出敏感字段 | 不导出身份证号、openid、密码、内部备注等敏感字段 |  |
| 防止无条件大批量导出 | 可限制最大导出条数或要求时间范围 |  |
| 导出接口不可匿名访问 | 必须鉴权 |  |
| 导出行为可审计 | 必须记录操作日志 |  |

---

## 7. 操作日志与审计检查

### 7.1 必须记录的行为

| 行为 | 是否记录 | 结果 |
|---|---|---|
| 政府账号登录 | 必须 |  |
| 查看统计页面 | 建议 |  |
| 查看服务记录详情 | 建议 |  |
| 查看证据链 | 建议 |  |
| 下载 / 预览合同 | 必须 |  |
| 导出补贴参考清单 | 必须 |  |
| 越权访问失败 | 建议 |  |

### 7.2 日志字段检查

| 字段 | 要求 | 结果 |
|---|---|---|
| operatorId | 操作人 ID |  |
| operatorName | 操作人名称 |  |
| roleCode | 操作角色 |  |
| module | 模块，例如“政府监管” |  |
| action | 动作，例如“导出补贴参考清单” |  |
| requestUrl | 请求接口 |  |
| requestParams | 关键查询条件，注意敏感信息脱敏 |  |
| ipAddress | 操作 IP |  |
| result | 成功 / 失败 |  |
| createTime | 操作时间 |  |

---

## 8. 接口统一返回与异常处理检查

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| 成功响应统一 | code、message、data 格式一致 |  |
| 业务错误清晰 | 如“无权限查看该区域数据” |  |
| 参数错误明确 | 如“startTime 不能晚于 endTime” |  |
| 未登录统一 401 | 所有受保护接口一致 |  |
| 无权限统一 403 | 所有越权接口一致 |  |
| 服务异常不暴露堆栈 | 前端不显示 Java 堆栈或 SQL 错误 |  |
| 日志记录异常 | 后端日志能定位错误 |  |

---

## 9. 前端通用检查

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| 页面路由受控 | 政府页面需要登录和权限 |  |
| 菜单权限正确 | GOV_ADMIN 只显示政府监管菜单 |  |
| 请求自动带 token | 所有接口携带 Authorization |  |
| 401 自动跳登录 | token 失效时跳转登录页 |  |
| 403 友好提示 | 无权限时显示提示页 |  |
| 表格分页正常 | 页码、每页数量可用 |  |
| 筛选重置正常 | 查询 / 重置按钮逻辑正确 |  |
| 导出按钮防重复点击 | 导出中禁用按钮或 loading |  |
| 文件预览失败提示 | 凭证加载失败有提示 |  |

---

## 10. 数据库与性能检查

| 检查项 | 判断标准 | 结果 |
|---|---|---|
| 常用筛选字段有索引 | service_type、provider_id、region_code、create_time 等 |  |
| 统计查询避免全表扫描 | 大表查询有时间或区域限制 |  |
| 列表接口分页 | 不允许一次返回全部数据 |  |
| 导出数量限制 | 大数据量导出有上限或异步预留 |  |
| JOIN 逻辑合理 | 避免因凭证多条导致统计重复 |  |
| Decimal 精度正确 | 处理量、面积、吨数精度正确 |  |
| 字典值统一 | 服务类型、状态枚举一致 |  |

---

## 11. 最终验收检查清单

完成第五个闭环后，必须能完整演示以下流程：

```text
1. 使用 GOV_ADMIN 账号登录 PC 后台。
2. 进入政府监管首页。
3. 查看服务统计数据。
4. 按时间、区域、服务类型筛选统计数据。
5. 打开服务记录列表。
6. 按服务方、服务类型、时间筛选服务记录。
7. 打开某条服务记录详情。
8. 查看该记录的证据链。
9. 预览作业照片。
10. 查看或下载合同。
11. 导出补贴参考清单 Excel。
12. 使用非政府账号访问政府统计接口，应返回 403。
13. 不登录访问政府统计接口，应返回 401。
14. 后台操作日志中能看到导出记录。
```

最终验收表：

| 验收项 | 是否通过 | 备注 |
|---|---|---|
| 政府账号可登录 |  |  |
| 政府菜单正确 |  |  |
| 服务统计正常 |  |  |
| 服务记录列表正常 |  |  |
| 证据链详情正常 |  |  |
| 凭证预览正常 |  |  |
| 补贴清单导出正常 |  |  |
| 未登录返回 401 |  |  |
| 越权访问返回 403 |  |  |
| 导出行为有日志 |  |  |
| 敏感信息未泄露 |  |  |
| 数据统计无重复 |  |  |

---

## 12. AI 检查输出模板

让 AI 检查代码后，建议按以下格式输出：

```text
# 代码检查结果

## 一、当前检查步骤
例如：服务统计接口 / 服务记录列表 / 证据链详情 / 补贴清单导出

## 二、功能实现情况
- 已实现：
- 未实现：
- 部分实现：

## 三、发现的问题
| 风险等级 | 问题位置 | 问题描述 | 影响 | 修改建议 |
|---|---|---|---|---|
| 高 / 中 / 低 | 文件名 + 方法名 | 问题说明 | 可能造成的后果 | 如何修改 |

## 四、权限与安全检查
- 401 校验：通过 / 不通过
- 403 校验：通过 / 不通过
- 数据越权：有风险 / 无明显风险
- 文件访问：有风险 / 无明显风险
- 导出安全：有风险 / 无明显风险

## 五、结论
可以进入下一步 / 需要修复后再进入下一步
```

---

## 13. 高风险问题快速判定

如果出现以下任意问题，不建议进入下一步：

```text
1. 政府接口未校验 token。
2. 政府接口未校验角色权限。
3. 政府账号可以修改或删除业务数据。
4. 农户或服务方可以访问政府统计接口。
5. 服务记录统计因 JOIN 凭证表导致重复统计。
6. 文件预览接口可以匿名访问。
7. 通过修改 fileId 可以查看其他记录的合同或照片。
8. 导出接口可以无条件导出全部数据且无权限校验。
9. 导出 Excel 包含密码、openid、身份证号等敏感字段。
10. current-user 返回角色错误或菜单错误。
```

---

## 14. 本闭环完成定义

当以下条件全部满足时，才认为“监管统计闭环”完成：

```text
1. GOV_ADMIN 可以正常登录。
2. GOV_ADMIN 只能看到政府监管菜单。
3. 政府端可以查看服务统计。
4. 政府端可以查看服务记录。
5. 政府端可以查看证据链和凭证。
6. 政府端可以导出补贴参考清单。
7. 非授权账号无法访问政府监管接口。
8. 导出和凭证查看行为可追溯。
9. 统计数据没有明显重复、漏算、错算。
10. 敏感信息没有明显泄露。
```
