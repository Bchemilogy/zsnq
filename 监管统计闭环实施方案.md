# 智枢农擎平台第五个闭环：监管统计闭环实施方案

## 一、闭环目标

第五个闭环的目标是打通政府监管端的核心能力：

```text
政府账号登录
↓
查看服务统计
↓
查看服务记录
↓
查看服务凭证
↓
导出补贴参考清单
```

该闭环完成后，政府监管人员可以通过 PC 端登录系统，查看区域内服务方、农户需求、服务记录、作业量、处理量、凭证材料等数据，并可按条件导出补贴参考清单。

本闭环不负责补贴审批，只提供数据支撑和证据链参考。补贴最终审批仍由政府主管部门线下或外部系统完成。

---

## 二、建设范围

### 1. 本阶段必须实现

```text
1. 政府账号登录
2. 政府监管端菜单权限
3. 监管统计首页
4. 服务统计查询
5. 服务记录查询
6. 服务记录详情查看
7. 凭证材料查看
8. 证据链详情查看
9. 补贴参考清单导出
10. 政府端只读权限控制
```

### 2. 本阶段暂不实现

```text
1. 补贴自动审批
2. 补贴资金发放
3. 在线支付结算
4. 复杂大屏动画
5. GIS 地图热力图
6. AI 自动审核凭证
7. 多部门协同审批流
8. 电子签章
```

---

## 三、端侧定位

### 1. PC 网页端

政府监管闭环优先通过 PC 网页端实现。

建议路径：

```text
https://你的域名/admin
```

政府账号登录后，只显示政府监管相关菜单。

### 2. 小程序端

本闭环主要面向政府监管人员，不建议在小程序端实现完整监管统计功能。

小程序只保留农户端和服务方端能力，不承担政府监管统计。

### 3. 数据大屏

第一阶段不单独建设正式数据大屏。

可以先在 PC 后台做一个“监管统计总览页”，后续再改造为正式大屏。

---

## 四、角色与权限设计

### 1. 角色定义

| 角色 | 角色编码 | 权限说明 |
|---|---|---|
| 政府监管人员 | GOV_ADMIN | 查看统计、查看服务记录、查看凭证、导出补贴参考清单 |
| 运营人员 | OPS_ADMIN | 查看和管理服务数据，可查看完整信息 |
| 超级管理员 | SUPER_ADMIN | 全部权限 |

### 2. 政府端权限原则

政府端应以“只读监管”为主。

```text
政府账号可以：
1. 查看统计数据
2. 查看服务方信息
3. 查看农户需求记录
4. 查看服务记录
5. 查看凭证材料
6. 导出补贴参考清单

政府账号不可以：
1. 审核服务方入驻
2. 修改服务方资料
3. 删除需求记录
4. 删除服务记录
5. 删除凭证材料
6. 修改系统配置
7. 管理用户角色
```

### 3. 权限编码建议

| 功能 | 权限编码 | GOV_ADMIN | OPS_ADMIN | SUPER_ADMIN |
|---|---|---:|---:|---:|
| 监管统计总览 | gov:overview:view | √ | √ | √ |
| 服务统计查看 | gov:stats:view | √ | √ | √ |
| 服务记录列表 | gov:record:list | √ | √ | √ |
| 服务记录详情 | gov:record:detail | √ | √ | √ |
| 凭证查看 | gov:evidence:view | √ | √ | √ |
| 补贴清单导出 | gov:subsidy:export | √ | √ | √ |
| 服务方审核 | provider:audit:approve | × | √ | √ |
| 服务记录删除 | service:record:delete | × | 可选 | √ |

---

## 五、PC 页面设计

### 1. 政府监管菜单结构

```text
政府监管
├── 监管统计总览
├── 服务统计分析
├── 服务记录查询
├── 凭证材料查看
└── 补贴参考清单
```

---

### 2. 监管统计总览页

路径建议：

```text
/gov/overview
```

页面目标：让政府人员快速了解区域内服务运行情况。

#### 页面展示指标

| 指标 | 说明 |
|---|---|
| 服务方总数 | 已审核通过的服务方数量 |
| 农户需求总数 | 农户提交的需求数量 |
| 已完成服务数 | 状态为 COMPLETED 的服务数量 |
| 服务记录数 | 已上传服务记录数量 |
| 作业面积合计 | 单位为亩的处理量合计 |
| 烘干处理量合计 | 单位为吨的处理量合计 |
| 凭证材料数量 | 照片、合同、收据等凭证总数 |
| 异常记录数量 | 缺少照片、缺少处理量、时间异常等记录 |

#### 图表建议

第一版使用普通图表即可：

```text
1. 服务类型分布柱状图
2. 区域服务数量柱状图
3. 月度服务趋势折线图
4. 服务状态饼图
5. 最近服务记录列表
```

---

### 3. 服务统计分析页

路径建议：

```text
/gov/statistics
```

#### 查询条件

```text
1. 时间范围
2. 区域
3. 服务类型
4. 服务方名称
5. 服务状态
```

#### 统计维度

| 维度 | 说明 |
|---|---|
| 按服务类型统计 | 农机、育秧、烘干、仓储、种植指导 |
| 按区域统计 | 县、乡镇、村 |
| 按服务方统计 | 每个服务方的服务次数、处理量 |
| 按时间统计 | 日、周、月趋势 |
| 按状态统计 | 已提交、已联系、服务中、已完成、已取消 |

#### 列表字段

| 字段 | 说明 |
|---|---|
| 服务类型 | 农机服务 / 烘干服务等 |
| 服务次数 | 完成服务记录数量 |
| 处理量 | 面积 / 吨数合计 |
| 服务方数量 | 参与服务的服务方数量 |
| 农户数量 | 涉及农户数量 |
| 凭证数量 | 上传凭证总数 |

---

### 4. 服务记录查询页

路径建议：

```text
/gov/service-record
```

#### 查询条件

```text
1. 服务记录编号
2. 农户姓名 / 联系电话
3. 服务方名称
4. 服务类型
5. 区域
6. 服务状态
7. 时间范围
8. 是否有照片
9. 是否有合同
```

#### 列表字段

| 字段 | 说明 |
|---|---|
| 记录编号 | service_record.id |
| 需求编号 | service_demand.id |
| 服务方名称 | provider_name |
| 农户联系人 | contact_name，可脱敏 |
| 服务类型 | service_type |
| 服务地址 | service_address |
| 处理量 | process_quantity + unit |
| 开始时间 | start_time |
| 结束时间 | end_time |
| 凭证数量 | evidence_count |
| 记录状态 | record_status |
| 操作 | 查看详情 / 查看证据链 |

---

### 5. 服务记录详情页

路径建议：

```text
/gov/service-record/detail/:recordId
```

页面内容：

```text
一、需求信息
- 需求编号
- 农户联系人
- 联系电话，政府端可脱敏
- 服务类型
- 服务地点
- 需求数量
- 期望时间
- 提交时间

二、服务方信息
- 服务方名称
- 联系人
- 联系电话
- 服务类型
- 服务区域
- 审核状态

三、服务记录
- 实际服务地点
- 实际处理量
- 单位
- 开始时间
- 结束时间
- 服务说明
- 提交时间

四、凭证材料
- 作业照片
- 合同 / 协议
- 收据 / 票据
- 其他附件

五、状态流转
- 需求提交时间
- 服务方联系时间
- 服务开始时间
- 服务完成时间
- 服务记录提交时间
```

---

### 6. 凭证材料查看页

路径建议：

```text
/gov/evidence
```

#### 查询条件

```text
1. 凭证类型
2. 服务方名称
3. 服务类型
4. 上传时间
5. 文件格式
```

#### 凭证类型

```text
WORK_PHOTO       作业照片
CONTRACT         合同 / 协议
RECEIPT          收据 / 票据
CONFIRM_PHOTO    双方确认照片
OTHER            其他材料
```

#### 页面能力

```text
1. 图片预览
2. PDF 合同查看或下载
3. 查看关联服务记录
4. 查看关联需求
5. 查看上传时间
```

---

### 7. 补贴参考清单页

路径建议：

```text
/gov/subsidy-reference
```

该页面用于按条件生成补贴参考数据。

#### 查询条件

```text
1. 时间范围
2. 区域
3. 服务类型
4. 服务方
5. 是否有作业照片
6. 是否有合同
7. 最小处理量
8. 最大处理量
```

#### 清单字段

| 字段 | 说明 |
|---|---|
| 序号 | 自动生成 |
| 服务方名称 | provider_name |
| 服务方联系人 | contact_name |
| 联系电话 | contact_phone，可按权限脱敏 |
| 服务类型 | service_type |
| 服务地点 | service_address |
| 农户联系人 | farmer_contact_name，可脱敏 |
| 处理量 | process_quantity |
| 单位 | 亩 / 吨 / 次 |
| 开始时间 | start_time |
| 结束时间 | end_time |
| 作业照片数量 | work_photo_count |
| 合同数量 | contract_count |
| 凭证完整性 | 完整 / 不完整 |
| 参考状态 | 可参考 / 待核查 |
| 备注 | 自动生成或人工备注 |

---

## 六、后端接口设计

### 1. 政府统计接口

```text
GET /api/gov/overview
GET /api/gov/statistics/service-type
GET /api/gov/statistics/region
GET /api/gov/statistics/provider
GET /api/gov/statistics/trend
```

### 2. 服务记录接口

```text
GET /api/gov/service-record/list
GET /api/gov/service-record/detail
```

### 3. 凭证材料接口

```text
GET /api/gov/evidence/list
GET /api/gov/evidence/detail
GET /api/gov/evidence/preview
```

### 4. 证据链接口

```text
GET /api/gov/evidence-chain/detail
```

### 5. 补贴参考清单接口

```text
GET  /api/gov/subsidy-reference/list
POST /api/gov/subsidy-reference/export
```

---

## 七、接口返回示例

### 1. 监管统计总览

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "providerCount": 128,
    "farmerDemandCount": 560,
    "completedServiceCount": 430,
    "serviceRecordCount": 430,
    "areaTotal": 12560.5,
    "dryingTotal": 860.3,
    "evidenceCount": 1320,
    "abnormalCount": 12
  }
}
```

### 2. 服务记录列表

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 1,
    "records": [
      {
        "recordId": 40001,
        "demandId": 30001,
        "providerName": "李四农机合作社",
        "farmerContactName": "张三",
        "serviceType": "AGRICULTURAL_MACHINERY",
        "serviceTypeName": "农机服务",
        "serviceAddress": "某县某乡镇某村",
        "processQuantity": 48,
        "unit": "亩",
        "startTime": "2026-05-01 09:00:00",
        "endTime": "2026-05-01 15:00:00",
        "evidenceCount": 3,
        "recordStatus": "SUBMITTED"
      }
    ]
  }
}
```

### 3. 证据链详情

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "demandInfo": {
      "demandId": 30001,
      "farmerContactName": "张三",
      "serviceTypeName": "农机服务",
      "serviceAddress": "某县某乡镇某村",
      "quantity": 50,
      "unit": "亩",
      "expectedTime": "2026-05-01 09:00:00"
    },
    "providerInfo": {
      "providerId": 10001,
      "providerName": "李四农机合作社",
      "contactName": "李四",
      "contactPhone": "13800000000"
    },
    "recordInfo": {
      "recordId": 40001,
      "processQuantity": 48,
      "unit": "亩",
      "startTime": "2026-05-01 09:00:00",
      "endTime": "2026-05-01 15:00:00",
      "description": "完成水稻收割服务"
    },
    "evidences": [
      {
        "evidenceType": "WORK_PHOTO",
        "fileName": "作业照片1.jpg",
        "fileUrl": "https://xxx.com/work1.jpg"
      },
      {
        "evidenceType": "CONTRACT",
        "fileName": "服务合同.pdf",
        "fileUrl": "https://xxx.com/contract.pdf"
      }
    ],
    "statusLogs": [
      {
        "action": "农户提交需求",
        "afterStatus": "SUBMITTED",
        "createTime": "2026-05-01 08:00:00"
      },
      {
        "action": "服务方上传服务记录",
        "afterStatus": "COMPLETED",
        "createTime": "2026-05-01 16:00:00"
      }
    ]
  }
}
```

---

## 八、数据库补充设计

第五个闭环主要复用前四个闭环的数据表：

```text
sys_user
sys_role
sys_user_role
service_provider
service_ability
service_demand
service_record
service_evidence
service_demand_log
sys_operation_log
```

建议新增一张导出记录表，方便追溯谁导出了补贴参考清单。

### 补贴参考导出记录表

```sql
CREATE TABLE subsidy_export_log (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '导出记录ID',
  export_user_id BIGINT NOT NULL COMMENT '导出用户ID',
  export_user_name VARCHAR(50) DEFAULT NULL COMMENT '导出用户名称',
  export_role VARCHAR(50) DEFAULT NULL COMMENT '导出角色',
  query_params TEXT COMMENT '导出查询条件JSON',
  file_name VARCHAR(255) DEFAULT NULL COMMENT '导出文件名',
  file_url VARCHAR(500) DEFAULT NULL COMMENT '导出文件地址',
  record_count INT DEFAULT 0 COMMENT '导出记录数量',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 九、补贴参考清单生成规则

### 1. 可进入补贴参考清单的记录

必须满足：

```text
1. 服务记录已提交。
2. 需求状态为 COMPLETED。
3. 服务方状态正常。
4. 服务方审核状态为 APPROVED。
5. 实际处理量大于 0。
6. 至少有 1 张作业照片。
```

### 2. 凭证完整性判断

| 条件 | 结果 |
|---|---|
| 有作业照片 + 有处理量 + 有开始结束时间 | 基本完整 |
| 有作业照片 + 有合同 + 有处理量 | 完整 |
| 无作业照片 | 待核查 |
| 处理量为空或为 0 | 待核查 |
| 结束时间早于开始时间 | 异常 |

### 3. 参考状态

```text
REFERENCEABLE    可参考
NEED_REVIEW      待核查
ABNORMAL         异常
```

---

## 十、导出 Excel 设计

### 1. 文件名称

```text
补贴参考清单_开始日期_结束日期.xlsx
```

示例：

```text
补贴参考清单_20260501_20260531.xlsx
```

### 2. Sheet 设计

建议包含两个 Sheet：

```text
Sheet1：补贴参考明细
Sheet2：统计汇总
```

### 3. Sheet1 字段

```text
序号
服务记录编号
需求编号
服务方名称
服务方联系人
服务方电话
服务类型
服务区域
服务地址
农户联系人
农户电话，脱敏
实际处理量
单位
开始时间
结束时间
作业照片数量
合同数量
凭证完整性
参考状态
备注
```

### 4. Sheet2 字段

```text
统计维度
统计对象
服务次数
处理量合计
服务方数量
农户数量
凭证数量
待核查数量
异常数量
```

---

## 十一、数据脱敏要求

政府端查看和导出时，应根据项目要求决定是否脱敏。

建议第一版默认脱敏农户手机号：

```text
13812345678 → 138****5678
```

服务方电话可以完整展示，也可以根据配置脱敏。

敏感证件类材料不建议在政府端直接展示完整原图，除非具备对应权限。

---

## 十二、实现步骤

### 第 1 步：确认数据基础

先确保前四个闭环已经有这些数据：

```text
1. 已审核通过的服务方
2. 服务方服务能力
3. 农户提交的需求
4. 服务方上传的服务记录
5. 服务凭证材料
6. 状态流转日志
```

没有这些数据，监管统计页面只能展示空数据。

---

### 第 2 步：配置政府端菜单和权限

新增菜单：

```text
政府监管
├── 监管统计总览
├── 服务统计分析
├── 服务记录查询
├── 凭证材料查看
└── 补贴参考清单
```

为 GOV_ADMIN 绑定权限：

```text
gov:overview:view
gov:stats:view
gov:record:list
gov:record:detail
gov:evidence:view
gov:subsidy:export
```

---

### 第 3 步：实现监管统计总览接口

实现：

```text
GET /api/gov/overview
```

统计内容：

```text
服务方总数
农户需求总数
已完成服务数
服务记录数
处理量合计
凭证数量
异常记录数量
```

---

### 第 4 步：实现服务统计分析接口

实现：

```text
GET /api/gov/statistics/service-type
GET /api/gov/statistics/region
GET /api/gov/statistics/provider
GET /api/gov/statistics/trend
```

先用 MySQL 聚合查询即可，不必引入数据仓库。

---

### 第 5 步：实现服务记录查询接口

实现：

```text
GET /api/gov/service-record/list
GET /api/gov/service-record/detail
```

重点支持分页和筛选。

---

### 第 6 步：实现证据链详情接口

实现：

```text
GET /api/gov/evidence-chain/detail?recordId=xxx
```

接口需要一次性返回：

```text
需求信息
服务方信息
服务记录信息
凭证材料
状态流转日志
```

---

### 第 7 步：实现凭证材料查看

实现：

```text
GET /api/gov/evidence/list
GET /api/gov/evidence/detail
```

支持图片预览和 PDF 查看 / 下载。

---

### 第 8 步：实现补贴参考清单查询

实现：

```text
GET /api/gov/subsidy-reference/list
```

按规则筛选出可参考、待核查、异常的服务记录。

---

### 第 9 步：实现补贴参考清单导出

实现：

```text
POST /api/gov/subsidy-reference/export
```

导出格式：`.xlsx`

导出后记录：

```text
导出人
导出时间
查询条件
导出数量
文件名称
```

写入：

```text
subsidy_export_log
```

---

### 第 10 步：开发 PC 页面

开发顺序建议：

```text
1. 监管统计总览页
2. 服务记录查询页
3. 服务记录详情页
4. 证据链详情页
5. 补贴参考清单页
6. Excel 导出按钮
```

---

### 第 11 步：权限和越权测试

重点测试：

```text
1. GOV_ADMIN 能访问政府监管页面。
2. GOV_ADMIN 不能访问服务方审核页面。
3. GOV_ADMIN 不能删除服务记录。
4. 未登录访问返回 401。
5. 无权限访问返回 403。
6. 导出操作有日志记录。
```

---

### 第 12 步：准备演示数据

至少准备：

```text
1. 3 个服务方
2. 5 条农户需求
3. 5 条服务记录
4. 每条服务记录至少 1 张作业照片
5. 至少 1 条有合同
6. 至少 1 条缺少照片，用于展示待核查
7. 至少 1 条时间异常，用于展示异常记录
```

---

## 十三、验收标准

| 验收项 | 标准 |
|---|---|
| 政府账号登录 | GOV_ADMIN 可正常登录 PC 后台 |
| 菜单权限 | 政府账号只看到政府监管菜单 |
| 统计总览 | 可看到服务方数、需求数、服务记录数、处理量等 |
| 服务统计 | 可按服务类型、区域、服务方统计 |
| 服务记录查询 | 可分页、筛选查看服务记录 |
| 服务记录详情 | 可查看需求、服务方、处理量、时间等 |
| 凭证查看 | 可查看作业照片、合同等材料 |
| 证据链查看 | 可看到需求、记录、凭证、状态日志完整链路 |
| 补贴清单查询 | 可按时间、区域、服务类型筛选 |
| Excel 导出 | 可导出补贴参考清单 |
| 导出日志 | 导出行为有记录 |
| 权限控制 | 政府账号不能审核、删除、修改业务数据 |
| 异常识别 | 缺照片、缺处理量、时间异常可标记待核查或异常 |

---

## 十四、最终演示流程

```text
1. 政府监管人员登录 PC 后台。
2. 进入“监管统计总览”。
3. 查看服务方总数、需求数、服务记录数、处理量合计。
4. 进入“服务统计分析”。
5. 按服务类型查看农机、烘干等服务数据。
6. 进入“服务记录查询”。
7. 筛选某个时间范围的服务记录。
8. 点击某条记录查看详情。
9. 查看作业照片、合同、处理量、开始结束时间。
10. 打开证据链详情。
11. 查看需求提交、服务开始、服务完成、凭证上传的完整链路。
12. 进入“补贴参考清单”。
13. 设置时间范围和服务类型。
14. 导出 Excel 补贴参考清单。
15. 系统记录导出日志。
```

---

## 十五、开发优先级

### P0 必须完成

```text
政府账号登录
政府端菜单权限
监管统计总览
服务记录查询
证据链详情
补贴参考清单导出
```

### P1 建议完成

```text
服务类型统计
区域统计
服务方统计
凭证材料独立查询
导出日志
异常标记
```

### P2 后续优化

```text
正式数据大屏
地图分布
趋势预测
AI 凭证审核
补贴审批流
多部门协同
```

---

## 十六、总结

第五个闭环的核心不是做一个漂亮的大屏，而是把前面四个闭环沉淀的数据转化成政府可查看、可追溯、可导出的监管数据。

第一版重点完成：

```text
政府登录
统计总览
服务记录查询
凭证查看
证据链查看
补贴参考清单导出
```

完成后，平台就具备了从“农户提交需求”到“服务方上传留痕”再到“政府监管统计”的完整业务闭环。
