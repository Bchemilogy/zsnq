# 需求（保留前两闭环 + 新增第三闭环：农户找服务）

## 本次需求（2026-04-29）
- 修复“联系电话登录”问题：手机号命中已审核通过申请后，不应因 `USERS` 中缺少服务方账号而登录失败。

## 一、第一闭环（保留）
登录认证闭环：登录 -> token -> current-user -> 角色跳转 -> 退出登录 -> 登录日志。

## 二、第二闭环（保留）
服务方入驻闭环：提交入驻 -> PC 审核 -> 状态回传 -> 通过后进入工作台。

## 三、第三闭环（新增）
目标：服务方维护服务能力，农户按分类找服务方并一键联系，系统记录联系行为。

### 3.1 业务链路
1. 服务方审核通过后进入工作台。
2. 服务方新增/编辑/启停自己的服务能力。
3. 农户选择服务分类查看服务方列表。
4. 农户查看服务方详情。
5. 农户一键联系（电话/微信）并记录联系日志。

### 3.2 服务方端需求
- 我的服务能力列表
- 新增/编辑服务能力
- 启用/停用服务能力
- 仅 APPROVED 且 NORMAL 的服务方可维护能力

### 3.3 农户端需求
- 服务分类页
- 服务方列表页（支持类型/区域/关键词/状态筛选）
- 服务方详情页
- 一键联系（电话/微信）
- 每次联系写入联系记录

### 3.4 PC 后台需求
- 服务能力管理页（查看、详情、下架）
- 联系记录页（查看列表、统计）

### 3.5 第三闭环接口需求
#### 服务方端
- `GET  /api/provider/ability/list`
- `POST /api/provider/ability/create`
- `POST /api/provider/ability/update`
- `POST /api/provider/ability/enable`
- `POST /api/provider/ability/disable`
- `GET  /api/provider/ability/detail`

#### 农户端
- `GET  /api/farmer/service/category/list`
- `GET  /api/farmer/provider/list`
- `GET  /api/farmer/provider/detail`
- `POST /api/farmer/contact-log/create`

#### PC 后台
- `GET  /api/admin/service-ability/list`
- `GET  /api/admin/service-ability/detail`
- `POST /api/admin/service-ability/disable`
- `GET  /api/admin/contact-log/list`
- `GET  /api/admin/contact-log/stat`


## 四、第三闭环本轮补充
- 农户服务方列表支持筛选：`serviceType`、`regionCode`、`keyword`、`availableStatus`。
- 列表排序规则：空闲优先、资料完整度高优先、最近更新优先。
- 后台服务能力列表支持 `serviceType/providerName/status` 筛选。
- 后台联系记录支持 `providerId/serviceType` 筛选。

## 五、第四闭环（新增）：需求与留痕闭环
- 农户提交需求（绑定服务方与服务能力）。
- 服务方查看并处理需求（已联系/服务中）。
- 服务方上传服务记录（处理量、时间、凭证）。
- 后台查看证据链（需求+记录+凭证+状态日志）。

## 本次需求（2026-04-30）
- 入驻申请页服务类型多选改为表格排版，勾选框与名称对齐。
- 入驻附件增加“其他文件”上传。
- “先上传附件”按钮文案改为“上传附件”，并说明其作用为提前上传并生成提交所需附件地址。
- 取消页面下方单独消息栏，仅保留弹窗提示。
- 我的入驻资料中的服务类型由英文改中文。
- 服务能力列表卡片仅展示“服务类型 - 能力名称”标题，后跟“查看详情”，点击进入原新增能力页面进行修改。
- 农户需求列表操作栏样式优化，支持同屏更密集展示多个项目。
- 服务方工作台当前状态改为中文显示。
- 补充数据持久化，避免隔天重启后账号/申请等数据丢失。
