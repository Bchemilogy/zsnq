# 需求（阶段目标：保留登录闭环 + 新增服务方入驻闭环）

## 一、第一闭环（保留）

目标：先打通登录认证链路，保证 PC 与小程序可联调。

链路：

1. 登录（PC 账号密码 / 小程序微信 code）
2. 获取 token
3. 查询 `current-user`
4. 按角色跳转
5. 退出登录
6. 后台可看到登录日志

第一闭环验收点（保持不变）：

- 小程序登录测试页可打开
- code 可传后端并换取 token
- token 可保存并可查询 `current-user`
- FARMER / PROVIDER 角色识别可用
- 可退出登录
- 后台能看到小程序登录日志

## 二、第二闭环（新增）

目标：服务方提交入驻资料，PC 审核，结果回传小程序，审核通过后才能进入服务方工作台。

链路：

1. 服务方登录小程序
2. 查询 `GET /api/provider/apply/status`
3. 按状态跳转：
   - `NOT_SUBMITTED` → 入驻申请页
   - `PENDING` → 审核中页
   - `APPROVED` → 服务方工作台
   - `REJECTED` → 驳回详情页
   - `DISABLED` → 禁用提示
4. 提交申请：`POST /api/provider/apply/submit`
5. PC 查看列表与详情
6. PC 审核通过/驳回
7. 小程序查看状态回传
8. 驳回后可修改并重提

## 三、小程序端需求

- 保留登录闭环页面与逻辑。
- 新增入驻闭环页面：
  - 入驻申请页
  - 审核状态页
  - 驳回详情页
  - 服务方工作台（仅 APPROVED 可进入）
- 附件上传（第一版允许模拟上传接口，返回 `fileUrl`）。

## 四、PC 后台需求

- 保留登录后台基础能力。
- 新增服务方入驻审核能力：
  - 入驻申请列表
  - 申请详情
  - 审核通过
  - 审核驳回（驳回原因必填）
  - 审核日志

## 五、接口需求（第二闭环新增）

### 小程序侧
- `GET  /api/provider/apply/status`
- `POST /api/provider/apply/submit`
- `GET  /api/provider/apply/detail`
- `POST /api/provider/apply/resubmit`
- `GET  /api/provider/workbench`

### PC 后台侧
- `GET  /api/admin/provider/apply/list`
- `GET  /api/admin/provider/apply/detail`
- `POST /api/admin/provider/apply/approve`
- `POST /api/admin/provider/apply/reject`
- `GET  /api/admin/provider/list`
- `GET  /api/admin/provider/detail`

### 上传接口
- `POST /api/file/upload`
