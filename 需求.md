# 需求（第二个闭环：服务方入驻闭环）

## 目标

实现“服务方在小程序提交入驻资料，PC 后台运营审核，审核结果回传小程序，审核通过后才能进入服务方工作台”的完整闭环。

## 闭环链路

1. 服务方登录小程序。
2. 小程序请求 `GET /api/provider/apply/status`。
3. 根据状态跳转：
   - `NOT_SUBMITTED` → 入驻申请页
   - `PENDING` → 审核中页
   - `APPROVED` → 服务方工作台
   - `REJECTED` → 驳回详情页
   - `DISABLED` → 禁用提示（第一版先提示）
4. 服务方提交入驻申请：`POST /api/provider/apply/submit`。
5. 运营在 PC 后台查看列表与详情，执行审核通过/驳回。
6. 小程序查询状态后显示审核结果；驳回可修改后重提。

## 小程序端需求

- 入驻状态判断与跳转。
- 入驻申请页（主体信息、服务类型、服务区域、服务说明、附件）。
- 审核状态页。
- 驳回详情页（展示驳回原因，可重提）。
- 服务方工作台（仅 `APPROVED` 可进入）。

## PC 后台需求

- 入驻申请列表。
- 入驻申请详情。
- 审核通过。
- 审核驳回（必须填写驳回原因）。
- 审核日志记录。

## 后端接口需求

### 小程序侧
- `GET  /api/provider/apply/status`
- `POST /api/provider/apply/submit`
- `GET  /api/provider/apply/detail`
- `POST /api/provider/apply/resubmit`
- `GET  /api/provider/workbench`

### PC 后台侧
- `GET  /api/admin/provider/apply/list`
- `GET  /api/admin/provider/apply/detail`
- `POST /api/admin/provider/apply/approve`
- `POST /api/admin/provider/apply/reject`
- `GET  /api/admin/provider/list`
- `GET  /api/admin/provider/detail`

### 文件上传
- `POST /api/file/upload`

## 验收重点

1. 服务方可提交入驻申请，状态变为 `PENDING`。
2. 运营可在 PC 后台查看并审核。
3. 审核通过变为 `APPROVED`，可进入工作台。
4. 审核驳回变为 `REJECTED`，可见驳回原因并可重提。
5. 全流程有审核日志记录。
