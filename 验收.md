# 第二个闭环验收说明（服务方入驻闭环）

## 运行方式

1. 启动服务

```bash
uv run uvicorn backend.main:app --reload
```

2. 执行闭环验收

```bash
uv run --no-project python scripts/acceptance_check.py --base-url http://127.0.0.1:8000
```

## 脚本验收项（10项）

1. 服务方状态接口可用（`GET /api/provider/apply/status`）
2. 服务方可提交入驻申请（`POST /api/provider/apply/submit`）
3. 运营账号可登录后台
4. PC 可查看入驻申请列表（`GET /api/admin/provider/apply/list`）
5. PC 可查看申请详情（`GET /api/admin/provider/apply/detail`）
6. 可驳回并保存原因（`POST /api/admin/provider/apply/reject`）
7. 小程序侧可看到驳回状态
8. 驳回后可重提（`POST /api/provider/apply/resubmit`）
9. 可审核通过（`POST /api/admin/provider/apply/approve`）
10. 通过后可进入工作台（`GET /api/provider/workbench`）


## 第三闭环补充验收
1. 农户列表筛选参数生效。
2. 农户列表排序满足：空闲优先、资料完整度高优先。
3. 后台服务能力筛选参数生效。
4. 后台联系记录筛选参数生效。

## 第四闭环验收清单（补充）
- 农户可从服务方详情页提交需求并绑定 providerId/abilityId。
- 服务方仅可查看和处理自己的需求（列表/详情）。
- 需求状态支持 SUBMITTED→CONTACTED→SERVICING→COMPLETED，且支持取消到 CANCELLED。
- 服务记录要求处理量>0、结束时间不早于开始时间、至少1张作业照片。
- 上传记录后需求状态自动变为 COMPLETED。
- 后台可查看需求列表/详情、服务记录列表/详情、证据链详情与凭证列表。
- 状态流转和记录提交均写入日志用于追溯。
