# 第一阶段验收说明

## 验收脚本

在服务启动后执行：

```bash
uv run --no-project python scripts/acceptance_check.py --base-url http://127.0.0.1:8000
```

## 验收项对应关系

1. 小程序登录测试页可打开（`GET /mini/login`）
2. 使用测试 code（模拟 `uni.login` 拿到 code）
3. code 传后端（`POST /api/auth/wechat-login`）
4. 后端返回 token
5. 本地保存 token（脚本中模拟 `mini_token`）
6. `GET /api/auth/current-user` 返回用户信息
7. 可识别 `FARMER/PROVIDER`
8. 可访问对应测试首页（`/mini/farmer-home` 或 `/mini/provider-home`）
9. 可退出登录（`POST /api/auth/logout`）
10. 后台可查看小程序登录日志（`GET /api/system/login-log` 且存在 `client_type=MINI`）
